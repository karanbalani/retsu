apiVersion: v1
kind: Service
metadata:
  name: retsu-api
  labels:
    app.kubernetes.io/name: retsu
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: retsu
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: retsu
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 2424
      targetPort: http
      protocol: TCP
